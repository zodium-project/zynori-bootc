---
# yaml-language-server: $schema=https://schema.blue-build.org/module-list-v1.json

modules:
  - type: dnf
    install:
      install-weak-deps: false
      packages:
          - cliphist
          - wtype
          - fastfetch
          - noctalia-qs
          - noctalia-shell
          - nwg-look
          - ddcutil
          - evolution-data-server
          - breeze-cursor-theme
          - papirus-icon-theme
          - papirus-icon-theme-dark

  - type: script
    snippets:
          - |
            mkdir -p /etc/zodium/icons /etc/zodium/icons/Papirus /etc/zodium/icons/Papirus-Dark
            mv /usr/share/icons/Papirus/* /etc/zodium/icons/Papirus/
            mv /usr/share/icons/Papirus-Dark/* /etc/zodium/icons/Papirus-Dark/
            rm -rf /usr/share/icons/Papirus-Dark
            rm -rf /usr/share/icons/Papirus
            ln -s /etc/zodium/icons/Papirus /usr/share/icons/
            ln -s /etc/zodium/icons/Papirus-Dark /usr/share/icons/